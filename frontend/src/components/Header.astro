---
---

<header>
    <div class="nav-container">
        <nav>
            <ul>
                <li><a href="index.php">Home</a></li>
                <li><a href="create.php">Create</a></li>
            </ul>
        </nav>
    </div>
    <div class="logo-container">
        <img src="/logo.png" alt="Gym Logo" class="logo">
    </div>
</header>

<style>
    :root {
        --background-dark: #13151a;
        --accent-color: #1DB954;
        --text-color: #FFFFFF;
        --nav-background: #333;
        --nav-hover-color: #1DB954;
        --logo-border-color: #1DB954;
    }

    body {
        background-color: var(--background-dark);
        color: var(--text-color);
        font-family: 'Roboto', sans-serif;
        margin: 0;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
    }

    header {
        background-color: var(--nav-background);
        padding: 20px 0;
        position: relative;
        text-align: center;
    }

    .nav-container {
        display: flex;
        justify-content: center;
    }

    nav ul {
        display: flex;
        gap: 20px;
        padding: 0;
        margin: 0;
        list-style: none;
    }

    nav ul li a {
        text-decoration: none;
        color: var(--text-color);
        font-size: 18px;
        transition: color 0.3s ease;
    }

    nav ul li a:hover {
        color: var(--nav-hover-color);
    }

    .logo-container {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 120px;
        height: 120px;
        border-radius: 50%;
        background-color: var(--background-dark);
        border: 4px solid var(--logo-border-color);
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .logo {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    @media (max-width: 768px) {
        nav ul {
            flex-direction: column;
            gap: 10px;
        }

        .logo-container {
            width: 80px;
            height: 80px;
        }
    }
</style>